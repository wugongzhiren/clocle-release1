<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>
    <declare-styleable name="NRecyclerView">
        <attr name="push_resistance" format="float"/>
        <attr name="over_resistance" format="float"/>
        <attr name="duration" format="integer"/>
        <attr name="pull_eable" format="boolean"/>
        <attr name="push_able" format="boolean"/>
        <attr name="over_scroll" format="boolean"/>
        <attr name="layout_color" format="color"/>
        <attr name="contentview_color" format="color"/>
        <attr name="inner_view" format="string"/>
        <attr name="loaddata_scrolleable" format="boolean"/>
        <attr name="interpolator" format="reference"/>
    </declare-styleable>
    <declare-styleable name="RatioImageView">
        <attr name="ratio" format="float"/>
    </declare-styleable>

    <!--图片间隔-->
    <declare-styleable name="NineGridLayout">
        <attr name="sapcing" format="dimension"/>
    </declare-styleable>
</resources>
